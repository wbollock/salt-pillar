# build-job:
#   tags:
#     - salt-master
#   stage: build
#   script:
#     - echo "Hello, $GITLAB_USER_LOGIN!"

test-job:
  tags:
    - salt-master
  stage: test
  script:
    - echo "Rendering pillar"
    - salt '*' pillar.items --local

deploy-prod:
  stage: deploy
  script:
    - cd /srv/pillar
    - git pull
